import { CardsGallery } from "@/components/cards-gallery/cards-gallery"

/** UI catalog of case-file card components (Alerts, Infant data, Mom data). */
export default function CardsPage() {
  return <CardsGallery />
}
